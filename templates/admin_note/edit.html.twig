{% extends 'base.html.twig' %}

{% block title %}Edit AdminNote{% endblock %}

{% block body %}
    <h1>Edit AdminNote</h1>

    {{ include('admin_note/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_note_index') }}"class="btn btn-light btn-lg btn-block">back to list</a>

    {{ include('admin_note/_delete_form.html.twig') }}
{% endblock %}
