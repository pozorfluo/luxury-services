{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>
    <a href="{{ path('app_register') }}"class="btn btn-success btn-sm">Create new</a>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td data-label="id">{{ user.id }}</td>
                <td data-label="Email">{{ user.email }}</td>
                <td data-label="Roles">{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td data-label="Password">{{ user.password }}</td>
                <td data-label="CreatedAt">{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td data-label="UpdatedAt">{{ user.updatedAt ? user.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td data-label="">
                    <a href="{{ path('user_show', {'id': user.id}) }}"class="btn btn-primary btn-sm btn-block">show</a>
                    <a href="{{ path('user_edit', {'id': user.id}) }}"class="btn btn-secondary btn-sm btn-block">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
